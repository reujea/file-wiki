//! 임베딩 벡터 파일 I/O — `fp-domain-types` crate로 추출됨 (cycle 7 step-d2).
//!
//! 호환성 re-export shim. 실제 정의는 `fp_domain_types::vec_io`.
//! 기존 `file_pipeline_core::domain::vec_io::*` 경로 유지.

pub use fp_domain_types::vec_io::*;
