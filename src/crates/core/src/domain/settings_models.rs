//! `SettingsDb` 도메인 모델 — `fp-domain-types` crate로 추출됨 (cycle 7 step-d2).
//!
//! 호환성 re-export shim. 실제 정의는 `fp_domain_types::settings_models`.
//! 기존 `file_pipeline_core::domain::settings_models::*` 경로 유지.

pub use fp_domain_types::settings_models::*;
