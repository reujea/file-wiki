//! 파이프라인 설정 데이터 타입 — `fp-domain-types` crate로 추출됨 (cycle 7 step-d2).
//!
//! 호환성 re-export shim. 실제 정의는 `fp_domain_types::config_models`.
//! 기존 `file_pipeline_core::domain::config_models::*` 경로 유지.

pub use fp_domain_types::config_models::*;
